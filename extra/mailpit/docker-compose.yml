services:
  mailpit:
    image: axllent/mailpit:latest
    container_name: mailpit
    restart: unless-stopped

    ports:
      - "1025:1025" # SMTP
      - "8025:8025" # Web UI

    networks:
      - postgres_shared_network

networks:
  postgres_shared_network:
    external: true
